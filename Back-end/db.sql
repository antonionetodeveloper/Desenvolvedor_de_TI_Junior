/* create table usuarios(
	ID int AUTO_INCREMENT, 
    nome varchar(30) NOT NULL,
	certificados varchar (20) NOT NULL,
    primary key (ID)
);
*/


/*
inert into usuarios (nome, certificados)
values (
    'Antônio', 'JIX'
);
 */